# Local config (Vercel CLI), generated artefacts that shouldn't ship
.vercel/
next.user-config.*

# Environment variables
.env*.local

# Common ignores
node_modules
.next/
.DS_Store
.claude/
tsconfig.tsbuildinfo
